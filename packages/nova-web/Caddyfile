:3000 {
	# Proxy API requests to the backend
	handle /api/* {
		reverse_proxy nova-engine:4000
	}

	# Proxy API docs to the backend
	handle /docs* {
		reverse_proxy nova-engine:4000
	}

	# SPA fallback for frontend routes
	handle {
		root * /srv
		try_files {path} /index.html
		file_server
	}
}
